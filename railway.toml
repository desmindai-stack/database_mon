[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/onprem/Dockerfile.backend"

[deploy]
startCommand = "sh -c 'if [ \"$RUN_MODE\" = worker ]; then python -m app.worker; else uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}; fi'"
