fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
asyncpg>=0.30.0
psycopg2-binary>=2.9.10
pydantic>=2.10.0
pydantic-settings>=2.6.0
apscheduler>=3.10.4
aiosqlite>=0.20.0
python-dotenv>=1.0.1
cryptography>=43.0.0
motor>=3.6.0
httpx>=0.27.0
aioodbc>=0.5.0
bcrypt>=4.1.0
pyjwt>=2.9.0
# Sağlık raporu PDF dışa aktarımı (Faz 17 İŞ 4). Saf Python — WeasyPrint'in aksine
# cairo/pango gibi sistem kütüphanesi gerektirmez, bu yüzden Dockerfile'a dokunmadan çalışır.
reportlab>=4.2.0

# SQL ayrıştırma (Faz 27 İŞ 1). Elle yazılmış regex, CTE adlarını ve takma adları gerçek
# tablo sanıp canlıda iki hataya yol açtı. sqlglot SAF PYTHON — pglast (libpg_query) daha
# doğru olurdu ama C eklentisi derlenmesi gerekiyor ve Dockerfile'a dokunmak proje kuralıyla
# yasak (gerekçe SORULAR.md'de).
sqlglot>=25.0.0
